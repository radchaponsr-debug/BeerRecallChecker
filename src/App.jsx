### ทางที่ 2: ใช้ Git Command (ถ้าเป็นวิศวกร)

```bash
# Clone repo
git clone https://github.com/[USERNAME]/BeerRecallChecker.git
cd BeerRecallChecker

# Create files
mkdir public src

# (สร้างไฟล์ทั้ง 5 ตามด้านบน)

# Upload
git add .
git commit -m "Initial commit: Add Beer Recall Checker app"
git push origin main
```
